/**
 * Auth Page - NextStep Platform
 * Thin wrapper - all logic lives in features/auth
 */

import { AuthPageContent } from "@/features/auth";

export default function AuthPage() {
    return <AuthPageContent />;
}
