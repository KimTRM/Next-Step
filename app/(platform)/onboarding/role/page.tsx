/**
 * Role Selection Page
 * Step 1 of onboarding - select user role
 * Thin page wrapper - logic in features/onboarding
 */

import { RoleSelectionContent } from "@/features/onboarding";

export default function RoleSelectionPage() {
    return <RoleSelectionContent />;
}
