/**
 * ============================================================================
 * FRONTEND - Messages Page
 * ============================================================================
 * 
 * Thin page wrapper that composes the Messages feature
 */

import { MessagesPageContent } from '@/features/messages/components';

export default function MessagesPage() {
    return <MessagesPageContent />;
}
