/**
 * Job Apply - Step 3: Profile
 * Route: /jobs/[id]/apply/profile
 */

import { ProfileStepPage } from "@/features/applications/components/apply-flow";

export default function ApplyProfilePage() {
    return <ProfileStepPage />;
}
